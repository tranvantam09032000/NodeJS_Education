<div class="mt-3">
    <div class="row">
        <div class="col-12 col-lg-3"></div>
         <div class="col-12 col-lg-9">
            <h2>{{course.name}}</h2>
            <iframe width="560" height="315" src="https://www.youtube.com/embed/{{course.videoId}}" frameborder="0"></iframe>
            <p>{{course.description}}</p>
         </div>
    </div>
</div>
